// variable : variable is used to store data ;

// To declare variable in javascript used let, Var , Const;

let a = 15;

var b = 10;

const c = 5;

// rules to declare variable
// 1. Variable can contain Number(digit), name(string), underscore;

// 2.we can't have space in variable or any other special char except dollar sign($);

// 3. variable can't start with number, it should be started with alphabet or _ ; 

// 4.varable shouldn't have keyword of variable it should have meaningful to understand the code;
